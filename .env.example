# Telegram Bot Token (from @BotFather)
BOT_TOKEN=

# Telegram User IDs allowed to use the bot (comma-separated for multiple)
ALLOWED_USER_ID=

# DeepSeek API (OpenAI-compatible) — optional for now
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Data directory (defaults to ./data)
DATA_DIR=./data

# Qdrant vector DB URL (defaults to http://localhost:6333)
QDRANT_URL=http://localhost:6333

# Embedding model name (HuggingFace Transformers.js)
EMBEDDING_MODEL=Xenova/multilingual-e5-small

# Web UI for drag-and-drop file upload (optional, defaults to port 3000)
# WEB_PORT=3000
# WEB_HOST=0.0.0.0
# WEB_PASSWORD=

# External URL for the web UI (used by Telegram "Upload" button)
# WEB_URL=http://REDACTED-HOST:3003
